{
  "scripts": {
    "dev": "run-p dev:elm dev:tauri",
    "dev:elm": "cross-env PORT=4000 'elm-app start --no-browser'",
    "dev:tauri": "sleep 2 && tauri dev"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5"
  }
}
